import styles from "./barmenuname.module.css";

const BarMenuName = () => {
	return (
		<div className={styles.container}>
			<span className={styles.title_one}>Animalitos</span>
			<span className={styles.title_two}>Toelettatura</span>
		</div>
	);
};

export default BarMenuName;
